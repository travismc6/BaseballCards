
import React, { Component } from 'react';

export class AddCard extends Component {

    constructor({ props }) {
        super(props);
        this.state = {  };
    }

    render() {
        return (
            <div>
                <h1>Add a new card</h1>
            </div>
        )}
}
